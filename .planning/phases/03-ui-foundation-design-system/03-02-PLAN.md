---
phase: 03-ui-foundation-design-system
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/App.tsx
  - src/components/ClipboardList.tsx
  - src/components/ClipboardItemCard.tsx
  - src/components/SearchBar.tsx
  - src/components/ContextMenu.tsx
  - src/components/ConfirmDialog.tsx
autonomous: false
requirements: [UIUX-01, UIUX-02, UIUX-06, UIUX-07]

must_haves:
  truths:
    - "Main window shows glassmorphism background with blur effect visible on all surfaces"
    - "Main window layout has a history list area and a placeholder area for AI tool panel per UIUX-02"
    - "Cards use rounded corners (14px radius), consistent padding, and glassmorphism styling"
    - "Window resizes gracefully with no content clipping or overflow per UIUX-06"
    - "All spacing is consistent using 4px grid from design tokens"
  artifacts:
    - path: "src/App.tsx"
      provides: "Main window shell with glassmorphism background, header, history list area, AI panel placeholder"
    - path: "src/components/ClipboardItemCard.tsx"
      provides: "Glassmorphism-styled card with rounded corners, hover effects"
    - path: "src/components/ClipboardList.tsx"
      provides: "Responsive scrollable list with glassmorphism styling"
  key_links:
    - from: "src/App.tsx"
      to: "src/styles/index.css"
      via: "glass/window-bg CSS classes"
      pattern: "glass|window-bg"
    - from: "src/components/ClipboardItemCard.tsx"
      to: "src/styles/index.css"
      via: "glass-card CSS class"
      pattern: "glass-card"
---

<objective>
Redesign the main window and all existing components with the Apple-aesthetic glassmorphism design system. Add AI tool panel placeholder area to the main window layout per UIUX-02. Ensure responsive layout with no overflow per UIUX-06.

Purpose: Transform functional Phase 1-2 components into the polished Apple-quality UI that defines the product's visual identity.
Output: All 6 existing components restyled with glassmorphism, App.tsx with dual-area layout (history + AI panel placeholder), responsive sizing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-clipboard-core-tray/01-UI-SPEC.md
@.planning/phases/03-ui-foundation-design-system/03-01-SUMMARY.md

<interfaces>
From src/App.tsx current structure:
```typescript
// Simple layout: header + ClipboardList, no AI panel area
export default function App() {
  // onMount: close-to-tray, startClipboardListener, loadHistory
  return (
    <div class="min-h-screen bg-gray-900 text-white flex flex-col">
      <header>...</header>
      <main><ClipboardList /></main>
    </div>
  );
}
```

From src/stores/clipboard.ts:
```typescript
export interface ClipboardItem {
  id: string; type: "text" | "image" | "file-paths";
  content: string; hash: string; timestamp: number;
  metadata: Record<string, unknown>; pinned: boolean; pinned_at?: number | null;
}
```

Design tokens available from Plan 01 (src/styles/design-tokens.css):
- .glass, .glass-subtle, .glass-card, .window-bg CSS classes
- CSS custom properties: --color-*, --radius-*, --blur-*, --space-*, --shadow-*
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign App.tsx main window with glassmorphism shell and dual-area layout</name>
  <files>src/App.tsx, src-tauri/tauri.conf.json</files>
  <read_first>src/App.tsx, src-tauri/tauri.conf.json, src/styles/index.css</read_first>
  <action>
1. Update `src-tauri/tauri.conf.json` window config:
   - Change width to 800, height to 600 for the main window (larger to accommodate AI panel area per UIUX-02)
   - Set `"transparent": true` to enable window-level glassmorphism backdrop (Tauri webview transparency)

2. Redesign `src/App.tsx` with Apple-aesthetic layout:
   - Replace `min-h-screen bg-gray-900` with `window-bg min-h-screen text-white flex flex-col`
   - Header: glass effect, display "ABoard" title with --font-display size, subtitle with --color-text-muted
   - Main content area: `flex flex-1 overflow-hidden` with two columns:
     - Left column (flex-1, ~65%): History list area with subtle separator border
     - Right column (w-[300px], collapsible placeholder): AI Tool Panel placeholder with "AI Tools" heading and "Coming soon..." muted text, styled with glass-subtle background
   - Add a thin divider between columns using --color-border-subtle
   - Ensure responsive: when window width < 700px, hide the AI panel area (CSS class approach: `hidden lg:block` on the right column)
   - Keep all existing functionality: close-to-tray, startClipboardListener, loadHistory

3. Header structure:
   ```
   +----------------------------------------------------------+
   | [ABoard icon/emoji] ABoard       Clipboard Manager       |
   +----------------------------------------------------------+
   | History (left, flex-1)    | AI Tools (right, 300px)      |
   |                           |                               |
   | SearchBar                 | (Coming soon...)              |
   | ClipboardList             |                               |
   |                           |                               |
   +----------------------------------------------------------+
   ```
  </action>
  <verify>
    <automated>cd /Users/clear2x/ai_ws/ABoard && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - App.tsx uses window-bg class for glassmorphism background
    - Layout has two-column structure: history area (left) + AI panel placeholder (right)
    - Header uses glass effect with consistent design tokens
    - AI panel area has "Coming soon..." placeholder text
    - tauri.conf.json window is 800x600 with transparent:true
    - Vite build passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Restyle all existing components with glassmorphism design tokens</name>
  <files>src/components/ClipboardItemCard.tsx, src/components/ClipboardList.tsx, src/components/SearchBar.tsx, src/components/ContextMenu.tsx, src/components/ConfirmDialog.tsx</files>
  <read_first>src/components/ClipboardItemCard.tsx, src/components/ClipboardList.tsx, src/components/SearchBar.tsx, src/components/ContextMenu.tsx, src/components/ConfirmDialog.tsx</read_first>
  <action>
Apply Apple-aesthetic restyling to all 5 components. Replace all hardcoded Tailwind color classes with design token CSS custom properties where possible, and add glassmorphism effects. Keep all existing functionality intact.

**ClipboardItemCard.tsx:**
- Replace card container with `glass-card p-3 transition-smooth` classes
- Border hover: replace hardcoded `border-gray-600/50` with `hover:border-[var(--color-border-hover)]`
- Type badge: keep semantic color coding (blue/green/purple) but use `rounded-[var(--radius-full)]`
- Pinned indicator: keep yellow dot
- Batch highlight: use `bg-[var(--color-accent)]/10 border-[var(--color-accent)]/50`
- Content text: `text-[var(--font-body)] text-[var(--color-text-secondary)]`
- Timestamp: `text-[var(--font-label)] text-[var(--color-text-muted)]`

**ClipboardList.tsx:**
- Outer container: remove hardcoded max-h, use `flex-1 overflow-y-auto p-2` for responsive sizing
- Gap between items: `gap-[var(--space-sm)]`
- Empty state: centered with `text-[var(--color-text-muted)]`
- Batch toolbar: glass-subtle background with consistent padding

**SearchBar.tsx:**
- Input: glass-subtle background, `rounded-[var(--radius-md)]`, `text-[var(--font-body)]`
- Focus ring: `focus:ring-1 focus:ring-[var(--color-accent)]`
- Placeholder: `placeholder:text-[var(--color-text-muted)]`

**ContextMenu.tsx:**
- Menu container: glass-card styling, `rounded-[var(--radius-md)] shadow-[var(--shadow-elevated)]`
- Menu items: hover with `bg-[var(--color-bg-card-hover)]`, `rounded-[var(--radius-sm)]`
- Delete item text: `text-[var(--color-destructive)]`

**ConfirmDialog.tsx:**
- Backdrop: `bg-black/60 backdrop-blur-sm`
- Dialog box: glass-card, `rounded-[var(--radius-lg)]`, `shadow-[var(--shadow-elevated)]`
- Confirm button: `bg-[var(--color-destructive)]` with hover state
- Cancel button: `bg-[var(--color-bg-card)]` with border
  </action>
  <verify>
    <automated>cd /Users/clear2x/ai_ws/ABoard && npx vite build 2>&1 | tail -5 && grep -c "glass" src/components/ClipboardItemCard.tsx src/components/ContextMenu.tsx src/components/ConfirmDialog.tsx</automated>
  </verify>
  <done>
    - All 5 components use design token CSS variables instead of hardcoded Tailwind colors
    - ClipboardItemCard uses glass-card class for glassmorphism card effect
    - ContextMenu and ConfirmDialog use glass-card with elevated shadows
    - SearchBar has glass-subtle background with focus ring
    - All components use consistent border-radius from tokens (var(--radius-*))
    - All existing functionality (search, context menu, batch delete, pin) still works
    - Vite build passes
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Main window glassmorphism redesign with dual-area layout (history + AI panel placeholder), all components restyled with design tokens</what-built>
  <how-to-verify>
    1. Run `npm run tauri dev` from project root
    2. Verify the main window has a translucent glassmorphism background (blurred, not opaque)
    3. Verify the layout shows two columns: history list on left, "AI Tools - Coming soon..." panel on right
    4. Verify cards have rounded corners with subtle glass effect and smooth hover transitions
    5. Verify search bar has glass-style background
    6. Right-click a card to verify context menu has glass-card styling with elevated shadow
    7. Try batch mode to verify ConfirmDialog has glass effect and blur backdrop
    8. Resize the window to verify no content clipping or overflow (UIUX-06)
    9. Verify spacing looks consistent (4px grid) and Apple-aesthetic
  </how-to-verify>
  <resume-signal>Type "approved" or describe visual issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS variables -> Webview | Static design tokens, no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | I | CSS custom properties | accept | CSS injection requires DOM access; CSP blocks inline styles except 'unsafe-inline' in style-src |
</threat_model>

<verification>
1. `grep "window-bg\|glass" src/App.tsx` confirms glassmorphism applied to main shell
2. `grep "glass-card" src/components/ClipboardItemCard.tsx` confirms card styling
3. `grep "glass-card\|glass-subtle" src/components/ContextMenu.tsx` confirms menu styling
4. `npx vite build` succeeds
5. Human visual verification of Apple-aesthetic quality
</verification>

<success_criteria>
- Main window has visible glassmorphism effect (blur + translucent bg)
- Two-column layout: history list + AI panel placeholder (per UIUX-02)
- All cards have rounded corners, glass-card effect, smooth hover transitions
- All spacing uses design token 4px grid
- Window resizes without clipping or overflow (UIUX-06)
- Human confirms Apple-quality visual standard (UIUX-07)
</success_criteria>

<output>
After completion, create `.planning/phases/03-ui-foundation-design-system/03-02-SUMMARY.md`
</output>
