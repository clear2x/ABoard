---
phase: 03-ui-foundation-design-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/index.css
  - src/styles/design-tokens.css
  - package.json
  - package-lock.json
autonomous: true
requirements: [UIUX-01, UIUX-07]

must_haves:
  truths:
    - "CSS design tokens define all colors, spacing, typography, and border-radius values used across the app"
    - "backdrop-filter: blur() CSS classes produce visible glassmorphism effect on elements"
    - "Framer Motion is installed and importable in Solid.js components"
  artifacts:
    - path: "src/styles/design-tokens.css"
      provides: "CSS custom properties for colors, spacing, typography, border-radius, blur"
      min_lines: 40
    - path: "src/styles/index.css"
      provides: "Tailwind import + design tokens import + glassmorphism utility classes"
      contains: "backdrop-filter"
  key_links:
    - from: "src/styles/index.css"
      to: "src/styles/design-tokens.css"
      via: "@import"
      pattern: "@import.*design-tokens"
---

<objective>
Install Framer Motion and create the Apple-aesthetic design system foundation: CSS custom property tokens for colors/spacing/typography/border-radius, and glassmorphism utility classes using backdrop-filter: blur().

Purpose: Establish the visual design language that all subsequent Phase 3 plans consume. Every component built in plans 02-04 references these tokens.
Output: design-tokens.css with complete token set, updated index.css with glassmorphism utilities, Framer Motion dependency installed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-clipboard-core-tray/01-UI-SPEC.md
@.planning/phases/03-ui-foundation-design-system/03-CONTEXT.md

<interfaces>
From src/styles/index.css:
```css
@import "tailwindcss";
```

From 01-UI-SPEC.md Color tokens:
- Dominant: #111827 (gray-900)
- Secondary: rgba(31,41,55,0.5) (gray-800/50)
- Accent: #3b82f6 (blue-500)
- Border tones: gray-700/50, gray-600/50, gray-800

From 01-UI-SPEC.md Typography:
- Body: 14px/400/1.625 (text-sm leading-relaxed)
- Label: 12px/400/1 (text-xs)
- Heading: 16px/600/1.375 (text-base font-semibold tracking-tight)
- Display: 20px/600/1.25 (text-xl font-semibold tracking-tight)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Framer Motion and create design token CSS</name>
  <files>package.json, package-lock.json, src/styles/design-tokens.css, src/styles/index.css</files>
  <read_first>src/styles/index.css, package.json</read_first>
  <action>
1. Install `motion` package (Framer Motion v11+ rebranded as "motion", Solid.js compatible via `motion/react` -- but for Solid.js use `motion` package with Solid bindings or `@motionone/solid` which is the official Motion One for Solid). Research: run `npm ls motion 2>/dev/null; npm ls @motionone/solid 2>/dev/null` to check. Install the correct Solid-compatible motion library:
   - Preferred: `npm install @motionone/solid` (Motion One for Solid.js, lightweight, tree-shakable, no React dependency)
   - Fallback if above not available: `npm install motion` and use the generic API
2. Create `src/styles/design-tokens.css` with CSS custom properties:
   ```
   :root {
     /* Colors - Dark theme */
     --color-bg-dominant: #111827;
     --color-bg-secondary: rgba(31, 41, 55, 0.5);
     --color-bg-card: rgba(31, 41, 55, 0.6);
     --color-bg-card-hover: rgba(31, 41, 55, 0.8);
     --color-bg-glass: rgba(17, 24, 39, 0.7);
     --color-border: rgba(75, 85, 99, 0.5);
     --color-border-hover: rgba(107, 114, 128, 0.5);
     --color-border-subtle: rgba(31, 41, 55, 1);
     --color-text-primary: #ffffff;
     --color-text-secondary: #d1d5db;
     --color-text-tertiary: #9ca3af;
     --color-text-muted: #6b7280;
     --color-accent: #3b82f6;
     --color-accent-hover: #60a5fa;
     --color-destructive: #ef4444;

     /* Typography */
     --font-body: 0.875rem;
     --font-label: 0.75rem;
     --font-heading: 1rem;
     --font-display: 1.25rem;
     --weight-body: 400;
     --weight-heading: 600;
     --leading-body: 1.625;
     --leading-heading: 1.375;
     --leading-display: 1.25;

     /* Spacing (4px base) */
     --space-xs: 4px;
     --space-sm: 8px;
     --space-md: 12px;
     --space-lg: 16px;
     --space-xl: 24px;
     --space-2xl: 32px;

     /* Border Radius */
     --radius-sm: 6px;
     --radius-md: 10px;
     --radius-lg: 14px;
     --radius-xl: 20px;
     --radius-full: 9999px;

     /* Glassmorphism */
     --blur-glass: 24px;
     --blur-subtle: 12px;
     --glass-bg: rgba(17, 24, 39, 0.6);
     --glass-border: rgba(255, 255, 255, 0.08);

     /* Shadows */
     --shadow-card: 0 2px 8px rgba(0, 0, 0, 0.3);
     --shadow-elevated: 0 8px 32px rgba(0, 0, 0, 0.4);

     /* Transitions */
     --duration-fast: 150ms;
     --duration-normal: 250ms;
     --duration-slow: 400ms;
     --ease-default: cubic-bezier(0.4, 0, 0.2, 1);
     --ease-spring: cubic-bezier(0.34, 1.56, 0.64, 1);
   }
   ```
3. Update `src/styles/index.css` to import the design tokens and add glassmorphism utility classes:
   ```css
   @import "tailwindcss";
   @import "./design-tokens.css";

   /* Glassmorphism utility classes per D-03 (CSS backdrop-filter) */
   .glass {
     background: var(--glass-bg);
     backdrop-filter: blur(var(--blur-glass));
     -webkit-backdrop-filter: blur(var(--blur-glass));
     border: 1px solid var(--glass-border);
   }

   .glass-subtle {
     background: rgba(17, 24, 39, 0.4);
     backdrop-filter: blur(var(--blur-subtle));
     -webkit-backdrop-filter: blur(var(--blur-subtle));
     border: 1px solid var(--glass-border);
   }

   .glass-card {
     background: var(--color-bg-card);
     backdrop-filter: blur(var(--blur-glass));
     -webkit-backdrop-filter: blur(var(--blur-glass));
     border: 1px solid var(--color-border);
     border-radius: var(--radius-md);
     box-shadow: var(--shadow-card);
   }

   /* Window background with glassmorphism */
   .window-bg {
     background: var(--color-bg-dominant);
     backdrop-filter: blur(40px);
     -webkit-backdrop-filter: blur(40px);
   }

   /* Smooth transitions utility */
   .transition-smooth {
     transition: all var(--duration-normal) var(--ease-default);
   }

   .transition-spring {
     transition: all var(--duration-slow) var(--ease-spring);
   }
   ```
  </action>
  <verify>
    <automated>cd /Users/clear2x/ai_ws/ABoard && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - design-tokens.css exists with :root block containing all color, typography, spacing, radius, glass, shadow, and transition tokens
    - index.css imports design-tokens.css and defines .glass, .glass-subtle, .glass-card, .window-bg utility classes with backdrop-filter
    - @motionone/solid (or equivalent Solid-compatible motion library) installed and listed in package.json dependencies
    - Vite build passes
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS -> Webview | Design tokens are static CSS, no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | E | Design tokens CSS | accept | Static CSS file, no runtime input, low-value target |
</threat_model>

<verification>
1. `cat src/styles/design-tokens.css | grep -c "^  --"` returns >= 20 (at least 20 CSS custom properties defined)
2. `grep "backdrop-filter" src/styles/index.css` confirms glassmorphism classes exist
3. `grep "design-tokens" src/styles/index.css` confirms import
4. `npx vite build` succeeds
</verification>

<success_criteria>
- Design token CSS file with complete Apple-aesthetic color palette, typography scale, spacing system, glassmorphism blur values, and transition timing
- Glassmorphism utility classes (.glass, .glass-card, .window-bg) using backdrop-filter per user decision D-03
- Motion library for Solid.js installed and ready for Plan 04 animation work
- Vite build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/03-ui-foundation-design-system/03-01-SUMMARY.md`
</output>
