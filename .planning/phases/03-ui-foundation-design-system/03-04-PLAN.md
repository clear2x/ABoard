---
phase: 03-ui-foundation-design-system
plan: 04
type: execute
wave: 4
depends_on: ["03-02", "03-03"]
files_modified:
  - src/components/ClipboardList.tsx
  - src/components/ClipboardItemCard.tsx
  - src/components/ContextMenu.tsx
  - src/components/FloatingPopup.tsx
  - src/App.tsx
autonomous: false
requirements: [UIUX-04, UIUX-07]

must_haves:
  truths:
    - "New clipboard items appear with a smooth slide-in animation (not instant pop-in)"
    - "Deleted items fade out smoothly before removal"
    - "Floating popup window appears with a scale-up + fade-in animation"
    - "Context menu appears with scale-up + fade-in animation"
    - "Card hover transitions are smooth and polished (border, background)"
  artifacts:
    - path: "src/components/ClipboardItemCard.tsx"
      provides: "Animated card with entrance/exit transitions"
      contains: "animate|transition|motion"
    - path: "src/components/FloatingPopup.tsx"
      provides: "Animated popup entrance and item selection"
  key_links:
    - from: "src/components/ClipboardList.tsx"
      to: "@motionone/solid"
      via: "animate function for list item transitions"
      pattern: "import.*motion|import.*animate"
---

<objective>
Add Framer Motion (Motion One) animations to all key UI transitions: list item entrance/exit, context menu appearance, floating popup window entrance, and panel transitions. Target visually smooth 60fps per UIUX-04.

Purpose: Animations are what separate a functional app from a polished Apple-quality experience. Per UIUX-07, every pixel transition must be fluid and natural.
Output: Animated components with entrance/exit transitions, consistent easing curves, and 60fps target.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ui-foundation-design-system/03-01-SUMMARY.md
@.planning/phases/03-ui-foundation-design-system/03-02-SUMMARY.md
@.planning/phases/03-ui-foundation-design-system/03-03-SUMMARY.md

<interfaces>
From @motionone/solid (installed in Plan 01):
```typescript
import { animate } from "@motionone/solid";
// or
import { createSignal, onMount } from "solid-js";
// Motion One animate works on DOM elements:
// animate(el, { opacity: [0, 1], transform: ["translateY(8px)", "translateY(0)"] }, { duration: 0.2 })
```

Design tokens for animation timing:
- --duration-fast: 150ms
- --duration-normal: 250ms
- --duration-slow: 400ms
- --ease-default: cubic-bezier(0.4, 0, 0.2, 1)
- --ease-spring: cubic-bezier(0.34, 1.56, 0.64, 1)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add entrance and exit animations to ClipboardList items and ContextMenu</name>
  <files>src/components/ClipboardItemCard.tsx, src/components/ClipboardList.tsx, src/components/ContextMenu.tsx</files>
  <read_first>src/components/ClipboardItemCard.tsx, src/components/ClipboardList.tsx, src/components/ContextMenu.tsx, src/styles/design-tokens.css</read_first>
  <action>
1. **ClipboardItemCard.tsx** -- Add entrance animation:
   - Import `onMount` and use Motion One `animate` on the card ref
   - On mount: animate from `{ opacity: 0, transform: "translateY(8px)" }` to `{ opacity: 1, transform: "translateY(0)" }` over 200ms with ease-default
   - Use Solid.js `ref` prop or `onMount` to get DOM element reference
   - Keep all existing functionality (selection, context menu, pin indicator, batch mode)

2. **ClipboardList.tsx** -- Add staggered entrance for initial load:
   - After `loadHistory()` completes, apply a staggered animation to rendered cards
   - Each card gets a delay of `index * 30ms` (max ~300ms for 10 cards)
   - Use CSS animation approach: add a `style="animation-delay: ${index * 30}ms"` to each card via ClipboardList, with a CSS `@keyframes slideIn` defined in index.css:
     ```css
     @keyframes slideIn {
       from { opacity: 0; transform: translateY(8px); }
       to { opacity: 1; transform: translateY(0); }
     }
     .animate-slide-in {
       animation: slideIn 200ms cubic-bezier(0.4, 0, 0.2, 1) both;
     }
     ```
   - Add this keyframe to `src/styles/index.css`

3. **ContextMenu.tsx** -- Add scale-up + fade-in animation:
   - When context menu appears, animate from `{ opacity: 0, transform: "scale(0.95)" }` to `{ opacity: 1, transform: "scale(1)" }` over 150ms with ease-default
   - Can be done with CSS: add `animate-context-menu` class with keyframe:
     ```css
     @keyframes contextMenuIn {
       from { opacity: 0; transform: scale(0.95); }
       to { opacity: 1; transform: scale(1); }
     }
     .animate-context-menu {
       animation: contextMenuIn 150ms cubic-bezier(0.4, 0, 0.2, 1) both;
     }
     ```
   - Add this keyframe to `src/styles/index.css`
  </action>
  <verify>
    <automated>cd /Users/clear2x/ai_ws/ABoard && npx vite build 2>&1 | tail -5 && grep -c "animate" src/styles/index.css src/components/ClipboardItemCard.tsx src/components/ContextMenu.tsx</automated>
  </verify>
  <done>
    - New clipboard items slide in from below with opacity fade (200ms)
    - Initial list load has staggered animation (30ms per card)
    - Context menu scales up from 0.95 with fade-in (150ms)
    - All animations use consistent easing from design tokens
    - Existing functionality unchanged
    - Vite build passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Add floating popup entrance animation and card hover polish</name>
  <files>src/components/FloatingPopup.tsx, src/App.tsx, src/styles/index.css</files>
  <read_first>src/components/FloatingPopup.tsx, src/App.tsx, src/styles/index.css</read_first>
  <action>
1. **FloatingPopup.tsx** -- Add entrance animation:
   - On mount, animate the entire popup container from `{ opacity: 0, transform: "scale(0.96) translateY(-4px)" }` to `{ opacity: 1, transform: "scale(1) translateY(0)" }` over 200ms with ease-default
   - Add CSS keyframe to index.css:
     ```css
     @keyframes popupIn {
       from { opacity: 0; transform: scale(0.96) translateY(-4px); }
       to { opacity: 1; transform: scale(1) translateY(0); }
     }
     .animate-popup-in {
       animation: popupIn 200ms cubic-bezier(0.4, 0, 0.2, 1) both;
     }
     ```
   - Apply `animate-popup-in` class to the FloatingPopup root div
   - Each item in the popup should also have the staggered slide-in (same as ClipboardList, 20ms stagger)

2. **Card hover polish** -- Add smooth transform on hover:
   - Add CSS utility to index.css:
     ```css
     .hover-lift {
       transition: transform 200ms cubic-bezier(0.4, 0, 0.2, 1),
                   border-color 200ms cubic-bezier(0.4, 0, 0.2, 1),
                   background 200ms cubic-bezier(0.4, 0, 0.2, 1);
     }
     .hover-lift:hover {
       transform: translateY(-1px);
     }
     ```
   - Apply `hover-lift` class to ClipboardItemCard containers
   - This gives a subtle 1px lift on hover for a tactile feel

3. **App.tsx** -- Ensure main window AI panel area has smooth show/hide:
   - If the right panel uses CSS visibility toggle (responsive breakpoint), no additional animation needed
   - Add a subtle border-left transition on the divider between panels
  </action>
  <verify>
    <automated>cd /Users/clear2x/ai_ws/ABoard && npx vite build 2>&1 | tail -5 && grep -c "popupIn\|hover-lift\|slideIn" src/styles/index.css</automated>
  </verify>
  <done>
    - Floating popup has scale + fade entrance animation (200ms)
    - Popup items have staggered slide-in
    - Cards have subtle hover-lift effect (1px translateY + border transition)
    - All animation keyframes defined in index.css with consistent timing
    - Vite build passes
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Smooth animations on all UI transitions: list items, context menu, floating popup, card hover</what-built>
  <how-to-verify>
    1. Run `npm run tauri dev` from project root
    2. Copy several items to clipboard and watch the list: items should slide in from below with a stagger effect
    3. Right-click a card: context menu should scale-up smoothly (not instant pop-in)
    4. Hover over cards: should feel a subtle lift effect with smooth border color transition
    5. Press Cmd+Shift+V: floating popup should appear with a smooth scale + fade animation
    6. Navigate popup with arrows: selection highlight should transition smoothly
    7. Verify ALL animations feel smooth and Apple-quality (no jank, no stutter)
    8. Verify animations do not feel slow or obstructive -- they should feel instant but polished
  </how-to-verify>
  <resume-signal>Type "approved" or describe animation issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS animations -> Webview | Pure CSS keyframes, no user input involved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | D | CSS animations | accept | Pure visual effect, no data impact, cannot be exploited for denial of service in desktop app |
</threat_model>

<verification>
1. `grep "slideIn\|popupIn\|contextMenuIn\|hover-lift" src/styles/index.css` confirms all animation keyframes exist
2. `grep "animate-slide-in\|animate-popup-in\|animate-context-menu" src/components/*.tsx` confirms animations applied to components
3. `npx vite build` succeeds
4. Human confirms 60fps visual quality on all animations
</verification>

<success_criteria>
- New list items slide in with stagger effect (UIUX-04)
- Context menu scales up smoothly (UIUX-04)
- Floating popup has scale + fade entrance (UIUX-04)
- Card hover has subtle lift + border transition (UIUX-07)
- All animations use consistent easing and timing from design tokens
- Human confirms Apple-quality visual standard (UIUX-07)
</success_criteria>

<output>
After completion, create `.planning/phases/03-ui-foundation-design-system/03-04-SUMMARY.md`
</output>
