---
phase: 04-ai-engine-integration
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src-tauri/src/ai/models.rs
  - src-tauri/src/ai/mod.rs
  - src-tauri/src/db.rs
  - src-tauri/src/lib.rs
autonomous: true
requirements: [AIEN-02, AIEN-03]

must_haves:
  truths:
    - "用户可查看已下载模型列表，包含模型名称、大小、状态"
    - "用户可下载新 GGUF 模型到本地 models 目录"
    - "用户可删除已下载模型并释放磁盘空间"
    - "用户可切换当前使用的模型"
    - "用户可调整温度、上下文长度、top_p 参数并立即生效"
  artifacts:
    - path: "src-tauri/src/ai/models.rs"
      provides: "模型 CRUD + 下载管理"
      exports: ["ModelManager", "ModelInfo", "ModelMetadata"]
    - path: "src-tauri/src/db.rs"
      provides: "model_metadata 表"
      contains: "model_metadata"
  key_links:
    - from: "src-tauri/src/ai/models.rs"
      to: "src-tauri/src/db.rs"
      via: "SQLite model_metadata 表"
      pattern: "model_metadata"
    - from: "src-tauri/src/ai/models.rs"
      to: "src-tauri/src/ai/local.rs"
      via: "加载指定路径的 GGUF 模型"
      pattern: "LocalProvider::load_model"
---

<objective>
模型管理和参数配置：在 SQLite 中存储模型元数据，实现下载/删除/切换模型的完整生命周期，支持温度等参数持久化。

Purpose: 用户需要管理本地 GGUF 模型文件（下载、切换、删除），以及调整推理参数。这是 AI 功能可用性的基础。
Output: models.rs 模型管理模块，db.rs 新增 model_metadata 表，4 个新 Tauri commands
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ai-engine-integration/04-CONTEXT.md

<!-- 依赖 Plan 01 的产出 -->
@.planning/phases/04-ai-engine-integration/04-01-SUMMARY.md

<interfaces>
<!-- Plan 01 创建的接口，本 Plan 需要 import -->

From src-tauri/src/ai/mod.rs (Plan 01 创建):
```rust
pub trait InferenceProvider: Send + Sync {
    async fn infer(&self, request: InferenceRequest) -> Result<InferenceResponse, String>;
    fn name(&self) -> &str;
    fn is_available(&self) -> bool;
}
pub struct AiState { /* 当前 provider + config */ }
pub struct InferenceRequest {
    pub prompt: String,
    pub system_prompt: Option<String>,
    pub max_tokens: Option<u32>,
    pub temperature: Option<f32>,
    pub top_p: Option<f32>,
}
```

From src-tauri/src/ai/config.rs (Plan 01 创建):
```rust
pub struct AiConfig {
    pub active_provider: ProviderType,
    pub model_path: Option<String>,
    pub context_length: u32,
    pub openai_api_key: Option<String>,
    pub openai_endpoint: String,
    pub openai_model: String,
    pub anthropic_api_key: Option<String>,
    pub anthropic_model: String,
}
```

From src-tauri/src/ai/local.rs (Plan 01 创建):
```rust
pub struct LocalProvider { /* llama-cpp-rs wrapper */ }
impl LocalProvider {
    pub fn load_model(path: &str) -> Result<Self, String>;
}
```

From src-tauri/src/db.rs:
```rust
pub struct DbState {
    pub conn: Mutex<rusqlite::Connection>,
}
pub fn init_db(app: &tauri::AppHandle) -> Result<(), Box<dyn std::error::Error>>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建模型元数据表 + ModelManager 模块</name>
  <files>src-tauri/src/db.rs, src-tauri/src/ai/models.rs</files>
  <read_first>src-tauri/src/db.rs, src-tauri/src/ai/mod.rs, src-tauri/src/ai/config.rs</read_first>
  <action>
1. 修改 src-tauri/src/db.rs:
   - 在 init_db 的 execute_batch 中添加 model_metadata 表:
     ```sql
     CREATE TABLE IF NOT EXISTS model_metadata (
         id TEXT PRIMARY KEY,
         name TEXT NOT NULL,
         filename TEXT NOT NULL,
         file_path TEXT NOT NULL,
         file_size INTEGER NOT NULL DEFAULT 0,
         status TEXT NOT NULL DEFAULT 'available',
         downloaded_at INTEGER NOT NULL,
         context_length INTEGER NOT NULL DEFAULT 2048,
         description TEXT
     );
     ```
   - 添加 `ensure_models_dir(app_data_dir: &Path) -> PathBuf` 辅助函数，创建 models/ 子目录

2. 创建 src-tauri/src/ai/models.rs:
   - 定义 `ModelInfo` struct（前端展示用）:
     ```rust
     #[derive(Debug, Clone, Serialize, Deserialize)]
     pub struct ModelInfo {
         pub id: String,
         pub name: String,
         pub filename: String,
         pub file_size: u64,        // bytes
         pub status: String,        // "available" | "downloading" | "error"
         pub downloaded_at: i64,
         pub is_active: bool,       // 是否为当前使用模型
     }
     ```
   - 定义 `ModelManager` struct 持有 models_dir: PathBuf
   - 实现方法:
     - `list_models(conn: &Mutex<Connection>) -> Vec<ModelInfo>`: 查询 model_metadata 表
     - `scan_models_dir(models_dir: &Path) -> Vec<ModelInfo>`: 扫描 .gguf 文件并与数据库交叉验证
     - `register_model(conn, models_dir, filename) -> Result<ModelInfo, String>`: 将新模型注册到数据库
     - `delete_model(conn, model_id) -> Result<(), String>`: 从数据库删除记录 + 删除文件
     - `switch_model(ai_state, model_id) -> Result<(), String>`: 设置当前活跃模型，重新加载 LocalProvider
  </action>
  <verify>
    <automated>cd /Users/clear2x/ai_ws/ABoard/src-tauri && cargo check 2>&1 | tail -5</automated>
  </verify>
  <done>
    - db.rs 包含 model_metadata 表创建 SQL
    - models.rs 导出 ModelInfo, ModelManager
    - list_models 返回所有已注册模型
    - delete_model 删除记录和文件
    - switch_model 重新加载 LocalProvider
  </done>
</task>

<task type="auto">
  <name>Task 2: 注册模型管理 + 参数配置 Tauri commands</name>
  <files>src-tauri/src/ai/mod.rs, src-tauri/src/lib.rs</files>
  <read_first>src-tauri/src/ai/models.rs, src-tauri/src/ai/config.rs, src-tauri/src/lib.rs</read_first>
  <action>
1. 修改 src-tauri/src/ai/mod.rs:
   - 添加 `pub mod models;`
   - 添加 4 个 Tauri command 函数:
     ```rust
     #[tauri::command]
     pub async fn ai_download_model(
         state: tauri::State<'_, AiState>,
         db_state: tauri::State<'_, DbState>,
         url: String,
         name: String,
     ) -> Result<ModelInfo, String>
     ```
     - 使用 reqwest 下载 GGUF 文件到 models/ 目录
     - 下载过程在 tauri::async_runtime::spawn 中执行
     - 下载完成后调用 models::register_model 注册到数据库
     - 发送 "model-download-progress" 事件通知前端进度

     ```rust
     #[tauri::command]
     pub async fn ai_delete_model(
         state: tauri::State<'_, AiState>,
         db_state: tauri::State<'_, DbState>,
         model_id: String,
     ) -> Result<(), String>
     ```

     ```rust
     #[tauri::command]
     pub async fn ai_get_config(
         state: tauri::State<'_, AiState>,
     ) -> Result<AiConfig, String>
     ```
     - 返回当前完整配置

     ```rust
     #[tauri::command]
     pub async fn ai_set_config(
         state: tauri::State<'_, AiState>,
         config: AiConfig,
     ) -> Result<(), String>
     ```
     - 更新配置并持久化到 ai-config.json
     - 如果 provider 类型改变，切换到新 provider
     - 如果参数（temperature, context_length, top_p）改变，立即生效

2. 修改 src-tauri/src/lib.rs:
   - 在 invoke_handler 中添加:
     ```rust
     ai::ai_download_model,
     ai::ai_delete_model,
     ai::ai_get_config,
     ai::ai_set_config,
     ```
  </action>
  <verify>
    <automated>cd /Users/clear2x/ai_ws/ABoard/src-tauri && cargo check 2>&1 | tail -5</automated>
  </verify>
  <done>
    - 4 个新 Tauri commands 注册到 invoke_handler
    - ai_download_model 从 URL 下载 GGUF 到 models/ 目录并注册
    - ai_delete_model 删除数据库记录和文件
    - ai_get_config 返回当前配置（含 temperature, context_length, top_p）
    - ai_set_config 持久化配置并热更新 provider
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend -> ai_download_model | 用户提供的 URL 下载源不可信 |
| Model files on disk | GGUF 文件可能被篡改 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | S | ai_download_model URL | mitigate | 仅允许 HTTPS URL，验证 Content-Type |
| T-04-06 | D | Model download | mitigate | 限制单文件大小 5GB，防止磁盘耗尽 |
| T-04-07 | T | Model file integrity | accept | GGUF 格式由 llama.cpp 校验，加载失败自动拒绝 |
</threat_model>

<verification>
1. `cargo check` 通过
2. `grep -r "ai_download_model\|ai_delete_model\|ai_get_config\|ai_set_config" src-tauri/src/lib.rs` 找到 4 个 command
3. `grep -r "model_metadata" src-tauri/src/db.rs` 找到表定义
4. `grep -r "temperature\|context_length\|top_p" src-tauri/src/ai/config.rs` 找到参数定义
</verification>

<success_criteria>
- 模型 CRUD 完整：下载、列表、切换、删除
- 参数配置可持久化：temperature, context_length, top_p
- ai-config.json 存储完整配置
- 所有文件操作异步执行不阻塞
</success_criteria>

<output>
After completion, create `.planning/phases/04-ai-engine-integration/04-02-SUMMARY.md`
</output>
